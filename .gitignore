# Python
venv/
__pycache__/
*.pyc

# Data artifacts — large, regenerated locally, NOT committed to the site repo
data/raw/
data/parquet/
data/features/

# OS
.DS_Store

# Note: web/data.js IS committed — it's the generated site data the deploy serves.
